# KMeans-Customer-Segmentation
This project uses the K-Means clustering algorithm to segment customers into groups based on income and spending behavior. The Elbow Method is used to determine the optimal number of clusters, and cluster centers are visualized to understand customer segments.
